# Environment file for docker-compose (containers)
# Keeps project .env unchanged for native development.

# Postgres connection used inside compose network
DATABASE_URL=postgres://myblog:myblog_pass@db:5432/myblog_db?sslmode=disable

# Redis URL (use service name `redis` in compose)
REDIS_URL=redis://redis:6379/0

# Port the app listens on inside container
LISTEN_PORT=8080

# When using sqlx in offline mode
SQLX_OFFLINE=1

# Replace with your ipstack key if used
IPSTACK_KEY=YOUR_IPSTACK_KEY_HERE
